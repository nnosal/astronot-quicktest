{
  "customizations": {
    "vscode": {
      "extensions": [
        "hverlin.mise-vscode",
        //"rgeraskin.mise",
		"astro-build.astro-vscode"
      ]
    }
  },
  "postCreateCommand": "curl https://mise.run | MISE_INSTALL_PATH=/usr/local/bin/mise sh && /usr/local/bin/mise install",
  "postStartCommand": "eval \"$(/usr/local/bin/mise activate --shims)\"",
}
